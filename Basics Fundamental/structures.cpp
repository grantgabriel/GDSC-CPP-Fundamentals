// Filename : structures.cpp

#include <bits/stdc++.h>             // Complete set of C++ library
using namespace std;                 // Namespaces to 
                                     // prevents variables name error

int main() {
    cout << "Hello World!" << endl;  // cout to print into the std output
                                     // endl to make a new line

    return 0;                        // exit condition, 0 = exit_success
                                     // 1 = exit_failure
}